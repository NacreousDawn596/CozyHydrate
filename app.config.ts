import { ExpoConfig } from "expo/config";

const config: ExpoConfig = {
  name: "CozyHydrate",
  slug: "cozyhydrate",
  web: {
    bundler: "metro",
    publicPath: "./",
  },
};

export default config;
